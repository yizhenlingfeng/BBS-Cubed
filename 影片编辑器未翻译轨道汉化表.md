# 影片编辑器未翻译轨道汉化表

> 生成日期：2026-09-14
> 范围：BBSFS 当前版本（bbs-fs-master + BBS++ gbeic.bbsplusplus + BBS-VFX/VFX-LIGHTS hardcoded）
> 说明：本表列出**时间轴上实际显示为轨道名、但 `KeyframeLocalizer` 与 L10n（`bbspp.keyframe.*`）均未覆盖**的条目。
> 已有翻译的轨道（约 576 条硬编码 + 604 条 L10n）不再列出。
>
> **实施状态：已完成（2026-09-14）**。以下 59 条已全部落地，双轨写入：
> - `KeyframeLocalizer.java` 硬编码兜底（CN / VFX_CN / ITEM_SPRAY_CN）
> - `bbspp/strings/zh_cn.json` L10n（优先级最高，用户可覆盖）
> - `bbspp/strings/en_us.json` 同步英文键
> - `bbs/assets/lang/zh_cn.json` 补 Clip 类型名 `release_use_item`

---

## 一、BBS 原版 Form 轨道

| # | 轨道 ID | 所属 Form | 字段类型 | 英文含义 | 建议中文翻译 | 建议落点 |
|---|---------|-----------|----------|----------|--------------|----------|
| 1 | `mobId` | MobForm | ValueString | Mob entity ID | 实体 ID | KeyframeLocalizer `CN` |
| 2 | `mobNbt` | MobForm | ValueString | Mob NBT data | 实体 NBT | KeyframeLocalizer `CN` |
| 3 | `scale` | FramebufferForm | ValueFloat | Framebuffer resolution scale | 分辨率缩放 | KeyframeLocalizer `CN` |

---

## 二、BBS++ 新增 / 注入 Form 轨道

### 2.1 FluidForm（流体形态）

> 走通用 `CN` 表（非 VFX / 非灯 / 非粒子分支）。

| # | 轨道 ID | 字段类型 | 英文含义 | 建议中文翻译 | 建议落点 |
|---|---------|----------|----------|--------------|----------|
| 4 | `mode` | ValueInt | Fluid mode (ocean/drop/procedural) | 流体模式 | `CN` |
| 5 | `flowSpeed` | ValueFloat | Flow speed | 流速 | `CN` |
| 6 | `turbulence` | ValueFloat | Turbulence amount | 湍流 | `CN` |
| 7 | `opacity` | ValueFloat | Opacity | 不透明度 | `CN` |
| 8 | `texture_tint` | ValueColor | Texture tint color | 纹理着色 | `CN` |
| 9 | `texture_whiten` | ValueFloat | Texture whiten amount | 纹理泛白 | `CN` |
| 10 | `physicsSensitivity` | ValueFloat | Physics sensitivity | 物理灵敏度 | `CN` |
| 11 | `subdivisions` | ValueInt | Mesh subdivisions | 细分度 | `CN` |
| 12 | `smoothShading` | ValueBoolean | Smooth shading | 平滑着色 | `CN` |
| 13 | `debug` | ValueBoolean | Debug draw | 调试 | `CN` |
| 14 | `sizeX` | ValueFloat | Ocean size X | X 尺寸 | `CN` |
| 15 | `sizeY` | ValueFloat | Ocean size Y | Y 尺寸 | `CN` |
| 16 | `sizeZ` | ValueFloat | Ocean size Z | Z 尺寸 | `CN` |
| 17 | `fillBlock` | ValueBoolean | Fill as block | 填充方块 | `CN` |
| 18 | `waveAmplitude` | ValueFloat | Wave amplitude | 波浪幅度 | `CN` |
| 19 | `waveFrequency` | ValueFloat | Wave frequency | 波浪频率 | `CN` |
| 20 | `surfaceTension` | ValueFloat | Surface tension | 表面张力 | `CN` |
| 21 | `dropSize` | ValueFloat | Water drop size | 水滴大小 | `CN` |
| 22 | `viscosity` | ValueFloat | Viscosity | 粘度 | `CN` |

### 2.2 ModelFormCMLMixin 注入轨道（CML / PBR 增强）

> 通过 Mixin 注入到 `ModelForm`，走通用 `CN` 表。
> `texture_tint` / `texture_whiten` 与 FluidForm 共用同一 id，翻译一次即可同时生效。

| # | 轨道 ID | 字段类型 | 英文含义 | 建议中文翻译 | 建议落点 |
|---|---------|----------|----------|--------------|----------|
| 23 | `actions_overlay` | ValueActionsConfig | Action overlay (CML) | 动作叠加层 | `CN` |
| 24 | `molangShared` | ValueBoolean | Share molang variables | Molang 变量共享 | `CN` |
| 25 | `pbr_s_r` | ValueFloat | PBR smoothness (R) | PBR 平滑度 | `CN` |
| 26 | `pbr_s_g` | ValueFloat | PBR metalness (G) | PBR 金属度 | `CN` |
| 27 | `pbr_s_b` | ValueFloat | PBR porosity (B) | PBR 孔隙度 | `CN` |
| 28 | `pbr_s_a` | ValueFloat | PBR emissive (A) | PBR 自发光 | `CN` |
| 29 | `pbr_n` | ValueFloat | PBR normal map strength | PBR 法线强度 | `CN` |

> 注：`actions_overlay0/1/...` 由 CML 动态追加，KeyframeLocalizer 已有数字后缀兜底逻辑，只需翻译 `actions_overlay` 主名即可。

### 2.3 ItemSprayForm（物品喷射形态）

| # | 轨道 ID | 字段类型 | 英文含义 | 建议中文翻译 | 建议落点 |
|---|---------|----------|----------|--------------|----------|
| 30 | `items` | ValueItemStacks | Spray item list | 喷射物品 | `ITEM_SPRAY_CN` |

---

## 三、摄像机 Clip 参数轨道

> Clip 展开后走 `form == null` 分支 → `localizeVFX(sheet.id)`，故建议落到 `VFX_CN` 或 L10n `bbspp.keyframe.vfx.*`。
> 字符串 / 表达式 / 通道组类型不显示为关键帧轨道，已剔除。

### 3.1 AudioClip（音频片段）

| # | 轨道 ID | 字段类型 | 英文含义 | 建议中文翻译 | 建议落点 |
|---|---------|----------|----------|--------------|----------|
| 31 | `audio` | ValueLink | Audio file | 音频文件 | `VFX_CN` |

### 3.2 SubtitleClip（字幕片段）

| # | 轨道 ID | 字段类型 | 英文含义 | 建议中文翻译 | 建议落点 |
|---|---------|----------|----------|--------------|----------|
| 32 | `size` | ValueFloat | Font size | 字号 | `VFX_CN` |
| 33 | `textShadow` | ValueBoolean | Text shadow toggle | 文字阴影 | `VFX_CN` |
| 34 | `windowX` | ValueFloat | Window anchor X | 窗口 X | `VFX_CN` |
| 35 | `windowY` | ValueFloat | Window anchor Y | 窗口 Y | `VFX_CN` |
| 36 | `backgroundOffset` | ValueFloat | Background padding offset | 背景偏移 | `VFX_CN` |
| 37 | `shadowOpaque` | ValueBoolean | Opaque shadow | 不透明阴影 | `VFX_CN` |
| 38 | `lineHeight` | ValueInt | Line height | 行高 | `VFX_CN` |
| 39 | `maxWidth` | ValueInt | Max text width | 最大宽度 | `VFX_CN` |
| 40 | `imageRight` | ValueBoolean | Image on right side | 图片居右 | `VFX_CN` |
| 41 | `imageScale` | ValueFloat | Image scale | 图片缩放 | `VFX_CN` |

> 注：`image`（ValueLink）不显示为关键帧轨道，不单列。

### 3.3 ComponentClip / ShakeClip（分量开关 / 摇晃）

| # | 轨道 ID | 字段类型 | 英文含义 | 建议中文翻译 | 建议落点 |
|---|---------|----------|----------|--------------|----------|
| 42 | `active` | ValueInt | Which components are active (bitmask) | 启用分量 | `VFX_CN` |
| 43 | `shake` | ValueFloat | Shake period (ticks) | 晃动周期 | `VFX_CN` |
| 44 | `shakeAmount` | ValueFloat | Shake amplitude | 晃动幅度 | `VFX_CN` |

### 3.4 DragClip（拖拽阻力）

| # | 轨道 ID | 字段类型 | 英文含义 | 建议中文翻译 | 建议落点 |
|---|---------|----------|----------|--------------|----------|
| 45 | `deterministic` | ValueBoolean | Deterministic drag | 确定性 | `VFX_CN` |
| 46 | `factor` | ValueFloat | Drag factor | 阻力系数 | `VFX_CN` |
| 47 | `rate` | ValueInt | Drag rate | 阻力速率 | `VFX_CN` |

### 3.5 DollyZoomClip（滑动变焦）

| # | 轨道 ID | 字段类型 | 英文含义 | 建议中文翻译 | 建议落点 |
|---|---------|----------|----------|--------------|----------|
| 48 | `focus` | ValueFloat | Focus distance | 焦点距离 | `VFX_CN` |

### 3.6 EntityClip（实体跟随）

| # | 轨道 ID | 字段类型 | 英文含义 | 建议中文翻译 | 建议落点 |
|---|---------|----------|----------|--------------|----------|
| 49 | `selector` | ValueInt | Entity selector index | 实体选择器 | `VFX_CN` |

### 3.7 LookClip（看向）

| # | 轨道 ID | 字段类型 | 英文含义 | 建议中文翻译 | 建议落点 |
|---|---------|----------|----------|--------------|----------|
| 50 | `relative` | ValueBoolean | Relative look | 相对 | `VFX_CN` |
| 51 | `atBlock` | ValueBoolean | Look at block | 看向方块 | `VFX_CN` |
| 52 | `forward` | ValueBoolean | Look forward | 朝向前方 | `VFX_CN` |

> 注：`block`（ValuePoint）展开后为 X/Y/Z 子键，走通用 `offset_x/y/z` 等已有翻译，不单列。

### 3.8 OrbitClip（绕对象旋转）

| # | 轨道 ID | 字段类型 | 英文含义 | 建议中文翻译 | 建议落点 |
|---|---------|----------|----------|--------------|----------|
| 53 | `copy` | ValueBoolean | Copy from current | 复制当前 | `VFX_CN` |
| 54 | `absolute` | ValueBoolean | Absolute orbit | 绝对 | `VFX_CN` |

### 3.9 TrackerClip（追踪器）

| # | 轨道 ID | 字段类型 | 英文含义 | 建议中文翻译 | 建议落点 |
|---|---------|----------|----------|--------------|----------|
| 55 | `look_at` | ValueBoolean | Look at target | 看向目标 | `VFX_CN` |
| 56 | `relative` | ValueBoolean | Relative tracking | 相对 | `VFX_CN` |
| 57 | `active` | ValueInt | Active components bitmask | 启用分量 | `VFX_CN` |

> 注：`group`（ValueString）不显示为关键帧轨道，不单列。

### 3.10 其他覆盖型 Clip

| # | 轨道 ID | 所属 Clip | 字段类型 | 英文含义 | 建议中文翻译 | 建议落点 |
|---|---------|-----------|----------|----------|--------------|----------|
| 58 | `additive` | KeyframeClip | ValueBoolean | Additive keyframe | 叠加 | `VFX_CN` |

> 注：
> - `IdleClip.position`（ValuePosition）展开为 x/y/z/yaw/pitch/roll/fov，均已在 `CN` / `VFX_CN` 中翻译。
> - `TranslateClip.translate`（ValuePoint）展开为 X/Y/Z，已翻译。
> - `MathClip.expression`（ValueExpression）不显示为关键帧轨道。
> - `CurveClip.channels`（ValueChannels）是通道容器，本身不显示为轨道名。

---

## 四、Clip 类型名缺失（L10n）

| # | 语言键 | 英文原文 | 建议中文翻译 | 文件 |
|---|--------|----------|--------------|------|
| 59 | `bbs.ui.camera.clips.bbs:release_use_item` | Release item | 释放物品 | `bbs-fs-master/src/client/resources/assets/bbs/assets/strings/zh_cn.json` |

---

## 五、汇总

| 分类 | 条目数 | 建议落点 |
|------|--------|----------|
| BBS 原版 Form | 3 | `KeyframeLocalizer.CN` |
| BBS++ FluidForm | 19 | `KeyframeLocalizer.CN` |
| ModelFormCMLMixin 注入 | 7 | `KeyframeLocalizer.CN` |
| BBS++ ItemSprayForm | 1 | `KeyframeLocalizer.ITEM_SPRAY_CN` |
| Clip 参数轨道 | 28 | `KeyframeLocalizer.VFX_CN` |
| Clip 类型名 | 1 | BBS 核心 `zh_cn.json` strings |
| **合计** | **59** | — |

> 说明：
> - `VFX_CN` 表本身是 BBS VFX 插件专用，但 `KeyframeTrackStyle.apply()` 在 `form == null`（摄像机 Clip）时统一走 `localizeVFX()`，因此 BBS 原版 Clip 参数也应落到此表。
> - 若希望更干净，可在 `KeyframeTrackStyle` 中为 Clip 轨道新增独立分支（如 `CLIP_CN`），并新增 `localizeClip()` 方法；本表暂不改动代码，只给翻译。
> - `active` 在 ComponentClip 与 TrackerClip 中复用同一 key，翻译一致即可。
> - `relative` 在 LookClip 与 TrackerClip 中复用，翻译一致即可。
> - `texture_tint` / `texture_whiten` 在 FluidForm、MiscForm（Mixin 注入）、ModelForm（Mixin 注入）中复用，翻译一次全局生效。
