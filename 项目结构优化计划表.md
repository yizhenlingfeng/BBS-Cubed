# BBS++ 项目结构优化计划表

> 生成时间：2026-09-03
> 当前版本：2.7.2
> 状态：**计划阶段，暂未执行任何更改**

---

## 一、项目现状速览

| 指标 | 数值 |
|------|------|
| Java 源文件 | 273 个 |
| 代码总行数 | 32,109 行 |
| Mixin 类总数 | 134 个 |
| 构建产物大小 | ~1.0 MB |
| 资源命名空间 | 4 个（bbs / bbspp / bbsvfx / xavin） |
| 顶层 Java 包 | 2 个（bbsplusplus / bbsppp） |

---

## 二、发现的问题与优先级

### 🔴 高优先级（影响可维护性，合并新模组前必须解决）

#### 1. 双顶层包并存：`bbsplusplus` 与 `bbsppp`
- **现状**：`gbeic.bbsppp` 包下有 22 个文件（纹理补间 TextureTween 功能），拥有独立的入口类 `BBSPPPModClient`、设置类、11 个 Mixin、导出器等。
- **问题**：这是从外部项目合并进来的功能，未融入主包结构。fabric.mod.json 中同时注册了两个 client entrypoint，两套 mixin 配置。
- **影响**：新增模组时容易继续"打补丁"式合并，包结构越来越乱。
- **建议**：将 `bbsppp` 全部内容迁移到 `bbsplusplus` 包下对应子包（texture 功能归入 `client.texture`，mixin 归入主 mixin 包），统一入口。

#### 2. Mixin 包扁平堆积：112 个文件直接平铺
- **现状**：`gbeic.bbsplusplus.mixin` 根包下直接放了 112 个 Mixin 类，只有兼容相关的（irlite/shadercurves/vfx/xavin）分了子包。
- **问题**：文件列表极长，无法按功能定位；新增 Mixin 时不知道该放哪。
- **建议**：按目标模块拆分子包，例如：
  - `mixin.ui` — UI 相关（约 60+ 个，可再细分 `ui.film` / `ui.keyframe` / `ui.form`）
  - `mixin.model` — 模型渲染相关
  - `mixin.film` — 影片/剪辑相关
  - `mixin.keyframe` — 关键帧相关
  - `mixin.render` — 渲染管线相关
  - `mixin.core` — 核心类（MinecraftClient、GameRenderer 等）

#### 3. 资源命名空间混乱：4 个命名空间 + 嵌套异常
- **现状**：
  - `assets/bbs/` — 30 个文件，借用了 BBS 主模组的命名空间，内含语言文件、方块状态、模型、着色器、纹理
  - `assets/bbspp/` — 3 个文件（音效）
  - `assets/bbsvfx/` — 8 个文件（VFX 着色器）
  - `assets/xavin/` — 1 个语言文件
  - **异常**：`assets/bbs/assets/lang/` 和 `assets/bbs/assets/bbsppp/lang/` 存在嵌套目录，Minecraft 资源加载不会识别 `assets/bbs/assets/` 下的内容
- **问题**：
  - 借用 `bbs` 命名空间可能与主模组资源冲突
  - 嵌套的 `assets/bbs/assets/` 路径下的大语言文件（54KB / 872行）可能根本没被加载
  - 多个命名空间分散，不利于统一管理
- **建议**：统一到 `assets/bbsplusplus/` 命名空间下，按子目录组织（lang / textures / shaders / sounds / models / blockstates），清理无效嵌套路径。

### 🟡 中优先级（影响开发效率，建议尽快处理）

#### 4. 工具类目录重复：`util` 与 `utils` 并存
- **现状**：
  - `util/`（6 文件）：ClipOverlapFixer、DoubleClickHelper、FilmAutoGameModeRestoreState、GameModeMessageSuppressor、GizmoModeController、IrisHelper
  - `utils/`（6 文件）：AlphanumComparator、EnvLogger、IIgnoreDepth、SettingsFileSanitizer、TextureThumbnailManager、XRayManager
- **建议**：合并为统一的 `util/` 目录，按功能再分子包（如 `util.render` / `util.ui` / `util.io`）。

#### 5. 渲染目录重复：`render` 与 `renderer` 并存
- **现状**：
  - `client/render/` — 仅 1 个文件（ModelUVTransformRuntime.java）
  - `client/renderer/` — 17 个文件（各类渲染器）
- **建议**：将 `render/` 中唯一文件移入 `renderer/`，删除空目录。

#### 6. 依赖版本硬编码在 build.gradle
- **现状**：fabric-api、iris、aaa-particles、architectury 等依赖版本直接写在 build.gradle 中，而平台版本（minecraft、yarn、loader）已在 gradle.properties 中管理。
- **建议**：将所有依赖版本号移到 gradle.properties，build.gradle 中引用变量，便于升级和维护。

#### 7. 大文件需拆分
| 文件 | 行数 | 建议 |
|------|------|------|
| AAAParticleFormRenderer.java | 1116 | 拆分为粒子状态管理 + 渲染逻辑 |
| KeyframeLocalizer.java | 931 | 按关键帧类型拆分本地化逻辑 |
| TextureTweenManager.java | 797 | 拆分为管理器 + 补间计算 + 缓存 |
| UIFilmSelectionPanelLibraryLayoutMixin.java | 795 | Mixin 逻辑提取到辅助类 |
| UIClipsMixin.java | 724 | 同上 |

### 🟢 低优先级（清理项，有空再做）

#### 8. 仓库根目录杂乱
- **`错误报告分析/`**（13 MB）：包含崩溃日志、debug.log、jar 解压文件等，不应提交到源码仓库。建议移到仓库外或加入 .gitignore 后删除。
- **`docs/`**：仅 1 张 AAA.png，可考虑补充项目文档或整理到 wiki。
- **`build/libs/`**：保留了 6 个历史版本 jar（已被 .gitignore 忽略，但占用本地空间），可定期清理。

#### 9. Mixin 配置文件可优化
- **现状**：6 个独立的 .mixins.json 配置文件（主 + 4 个兼容 + bbsppp）。
- **建议**：包结构重组后，可合并为更少的配置文件，或按功能模块重新划分。每个 mixin 配置文件对应一个逻辑模块。

#### 10. 性能优化方向（合并更多模组后）
- **Mixin 启动开销**：134 个 Mixin 类会增加启动时间，可评估哪些可以用事件回调替代
- **条件加载**：兼容类 Mixin 已有条件加载（通过 MixinPlugin），主 Mixin 也可评估是否需要
- **渲染热路径**：AAAParticleFormRenderer、ItemSpray 等渲染类可优化对象分配

---

## 三、分阶段执行计划

### 阶段一：结构清理（低风险，先做）
**目标**：消除重复目录，清理仓库垃圾，不改变任何功能逻辑

| 步骤 | 操作 | 预计改动 | 风险 |
|------|------|----------|------|
| 1.1 | 删除 `错误报告分析/` 目录（备份到仓库外） | 删 13MB | 无 |
| 1.2 | 合并 `client/render/` → `client/renderer/` | 移动 1 文件 + 改 import | 低 |
| 1.3 | 合并 `util/` 与 `utils/` → 统一 `util/` | 移动 12 文件 + 改 import | 低 |
| 1.4 | 清理 build/libs 历史版本 | 删 5 个旧 jar | 无 |

**验证**：每次移动后执行 `gradlew build` 确保编译通过。

---

### 阶段二：包结构统一（中风险，核心步骤）
**目标**：合并 bbsppp 包，重组 mixin 子包

| 步骤 | 操作 | 预计改动 | 风险 |
|------|------|----------|------|
| 2.1 | 将 `gbeic.bbsppp` 下非 mixin 类迁移到 `gbeic.bbsplusplus` 对应子包 | 移动 11 文件 + 改包名 + 改 import | 中 |
| 2.2 | 将 `bbsppp.mixin` 下 11 个 Mixin 迁移到主 mixin 包，更新 `bbsppp.mixins.json` → 合并到主配置 | 移动 11 文件 + 改 mixin 配置 | 中 |
| 2.3 | 统一 client entrypoint：将 BBSPPPModClient 的初始化逻辑合并到 BBSPlusPlusModClient | 改 2 个入口类 + fabric.mod.json | 中 |
| 2.4 | 将主 mixin 包 112 个文件按功能拆分子包（ui / model / film / keyframe / render / core） | 移动 112 文件 + 改 6 个 mixin 配置 | 中高 |

**验证**：
- 每步后 `gradlew build` 编译通过
- 启动游戏测试核心功能（关键帧编辑、影片录制、纹理补间等）
- 检查日志中无 Mixin 应用失败警告

---

### 阶段三：资源整理（中风险）
**目标**：统一资源命名空间，清理无效路径

| 步骤 | 操作 | 预计改动 | 风险 |
|------|------|----------|------|
| 3.1 | 确认 `assets/bbs/assets/lang/` 下的语言文件是否被加载（对比运行时翻译效果） | 调查 + 可能移动文件 | 中 |
| 3.2 | 将所有资源迁移到 `assets/bbsplusplus/` 统一命名空间 | 移动 42 文件 + 改代码中资源引用路径 | 中高 |
| 3.3 | 更新 fabric.mod.json 中的 icon 路径等引用 | 改配置 | 低 |
| 3.4 | 清理空的旧命名空间目录 | 删 4 个空目录 | 无 |

**验证**：
- 游戏内所有文本显示正常（中文/英文）
- 所有纹理、着色器、音效正常加载
- 物品模型、方块状态正常

---

### 阶段四：构建配置与代码质量（低风险）
**目标**：优化构建脚本，拆分大文件

| 步骤 | 操作 | 预计改动 | 风险 |
|------|------|----------|------|
| 4.1 | 将 build.gradle 中所有依赖版本移到 gradle.properties | 改 2 文件 | 低 |
| 4.2 | 拆分 AAAParticleFormRenderer（1116行） | 拆 1→3 文件 | 中 |
| 4.3 | 拆分 KeyframeLocalizer（931行） | 拆 1→2-3 文件 | 中 |
| 4.4 | 评估 Mixin 配置文件合并 | 改配置 | 低 |

---

### 阶段五：性能优化（合并更多模组后按需进行）
**目标**：控制模组体积和启动开销

| 步骤 | 操作 | 说明 |
|------|------|------|
| 5.1 | 评估 Mixin → 事件替换 | 减少运行时字节码修改 |
| 5.2 | 渲染热路径对象池化 | 减少 GC 压力 |
| 5.3 | 资源压缩检查 | PNG 优化、OGG 码率检查 |
| 5.4 | 按需加载兼容模块 | 未安装对应模组时不加载兼容代码 |

---

## 四、合并新模组的规范建议

为避免重蹈"打补丁式合并"的覆辙，后续合并新功能/模组时应遵循：

1. **统一包名**：所有代码必须放在 `gbeic.bbsplusplus` 下，按功能归入对应子包，不保留原项目包名
2. **Mixin 分类**：新增 Mixin 必须放入对应功能子包，不允许直接堆在 mixin 根包
3. **资源统一**：所有资源放在 `assets/bbsplusplus/` 下，不新增命名空间
4. **入口统一**：不新增独立 entrypoint，初始化逻辑注册到统一的 ModClient 中
5. **版本管理**：新依赖版本号写入 gradle.properties，不硬编码

---

## 五、风险与回滚

- **所有阶段均建议在 Git 分支上进行**，每完成一个阶段提交一次，便于回滚
- 阶段二（包结构统一）和阶段三（资源整理）风险最高，建议每个子步骤后都运行游戏验证
- 移动文件时优先使用 IDE 的 Refactor → Move 功能，自动更新所有 import 和引用
- Mixin 配置文件中的类名路径必须与实际包路径一致，否则 Mixin 不会生效

---

*本计划表仅为规划文档，尚未对项目进行任何实际更改。*
