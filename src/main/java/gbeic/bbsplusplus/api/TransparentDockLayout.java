package gbeic.bbsplusplus.api;

/** Allows a dock to leave its base transparent for a shared viewport rendered behind it. */
public interface TransparentDockLayout
{
    void bbspp_cml$setTransparentBase(boolean transparent);
}
