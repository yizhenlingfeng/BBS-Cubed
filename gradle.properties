# BBS++ 构建配置（Minecraft 1.20.1 / Fabric）
# 只集中本模组自身的坐标与平台版本；外部依赖 mod 的版本仍写在 build.gradle 的 dependencies 里。

# 平台版本
minecraft_version=1.20.1
yarn_mappings=1.20.1+build.10
loader_version=0.16.14
java_version=17

# fabric.mod.json 中声明的依赖范围
minecraft_version_range==1.20.1
loader_version_range=>=0.15.0
java_version_range=>=17

# 模组坐标
mod_id=bbsplusplus
mod_name=BBS++
mod_version=2.6.3
mod_group_id=gbeic
mod_license=LGPL-3.0-or-later
archives_base_name=BBS++

# 构建插件
loom_version=1.15.5
