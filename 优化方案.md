# BBS-PlusPlus 项目优化方案（已实施）

> 生成时间：2026-09-04  
> 项目规模：502 个 Java 文件 / 67,061 行代码 / 234 个 Mixin 类  
> 构建验证：`gradlew compileJava` **BUILD SUCCESSFUL**

---

## 一、代码健康度检查结果

| 检查项 | 结果 | 状态 |
|--------|------|------|
| Mixin 类注册完整性 | 234/234 全部已注册 | ✅ 正常 |
| 未注册死代码 Mixin | 0 个 | ✅ 正常 |
| 注释占比 | 6.9%（4,597 行） | ✅ 合理 |
| 空行占比 | 15.8%（10,581 行） | ✅ 合理 |
| TODO/FIXME 标记 | 0 个真正标记 | ✅ 正常 |
| 重复 Mixin 类名 | 10 组同名（分属 common/client 不同包，内容不同） | ✅ 正常架构 |

**结论：源代码本身结构良好，无死代码、无未注册 Mixin，代码整洁度达标。**

---

## 二、已执行的清理操作

### 2.1 删除临时/调试文件

| 文件/目录 | 大小 | 说明 | 状态 |
|-----------|------|------|------|
| `java_pid21496.hprof` | 684 MB | JVM 内存转储文件，调试遗留 | ✅ 已删除 |
| `issue_screenshot.png` | 459 KB | Issue 报告截图 | ✅ 已删除 |
| `issue_screenshot2.png` | 291 KB | Issue 报告截图 | ✅ 已删除 |
| `_memcheck/` | 8.4 MB | 内存检查工具目录（含 .class 和 temp.jar） | ✅ 已删除 |
| `mchorse/` | 0.04 MB | 反编译的 .class 文件目录 | ✅ 已删除 |

### 2.2 清理构建/运行产物

| 目录 | 大小 | 说明 | 状态 |
|------|------|------|------|
| `build/` | 51 MB | 编译输出目录（可重新生成） | ✅ 已清理 |
| `.gradle/` | 245 MB | Gradle 缓存（可重新下载） | ✅ 已清理 |
| `run/` | ~1 GB | Minecraft 运行时目录（已重建目录结构并恢复 BBS 依赖 jar） | ✅ 已清理 |
| `.idea/` | 0.02 MB | IntelliJ IDEA 配置 | ✅ 已清理 |

### 2.3 资源文件处理（重要修正）

| 路径 | 处理方式 | 说明 |
|------|----------|------|
| `assets/bbs/assets/lang/` | **已恢复** | ⚠️ 此前误判为无用资源，实际是 BBS L10n 系统的标准语言文件路径（由 `L10nMixin` 通过 `Link.assets("lang/<lang>.json")` 加载），删除会导致所有设置项和 UI 文本翻译失效 |
| `assets/bbs/assets/bbsppp/lang/` | **已恢复** | FSloveCML 相关语言文件 |
| `assets/bbs/assets/textures/AAA.png` | 已删除 | 未被代码引用的纹理 |
| `assets/bbs/assets/textures/itemspray.png` | 已删除 | 未被代码引用的纹理 |

> **教训**：`assets/bbs/assets/` 是 BBS 模组自建 L10n 系统的资源根路径，不是标准 Minecraft 资源路径，但被 BBS 的 `Link.assets()` 方法使用。删除前必须确认代码引用关系。

### 2.4 更新 .gitignore

已添加以下规则，防止未来再次提交临时文件：
```
# Debug / temporary files
*.hprof
issue_screenshot*.png
_memcheck/
mchorse/
```

---

## 三、代码修改（3.1.1）

### 功能移除
- 移除姿势编辑器中的**纹理调色（texture_tint）**和**纹理美白（texture_whiten）**功能
- 删除 `PoseTextureGradeEditorHolder` API 接口
- 精简 `UIPoseEditorMixin` 与 `UIPoseKeyframeFactoryMixin` 中约 200 行相关代码

### Bug 修复
- 修复 OrbitViewGizmo 在原版轨道模式下被误隐藏的 bug
  - 根因：3.1 新增的 `@Shadow controller` HEAD NPE 守卫存在误判风险（controller 实际不会为 null），导致 `isActive()` 被强制返回 false
  - 修复：移除该守卫，改用 `@Accessor` 安全读取目标类字段

### 功能增强
- 跟随轨道模式（模式 6）新增 **perspective 右键菜单**支持
  - 在跟随轨道模式下显示与原版轨道相同的右键选项（传送中心到录制点、附加轨道、切换正交）
  - 此前仅原版轨道模式（模式 2）有此菜单

---

## 四、执行清单

- [x] 代码健康度全面检查（502 文件 / 234 Mixin）
- [x] 删除临时/调试文件（hprof、截图、_memcheck、mchorse）
- [x] 清理构建产物（build、.gradle、run、.idea）
- [x] 恢复被误删的语言文件（assets/bbs/assets/lang/、bbsppp/lang/）
- [x] 删除未引用的纹理文件（AAA.png、itemspray.png）
- [x] 更新 .gitignore 防止回归
- [x] 恢复构建依赖（BBS 主 jar）
- [x] 验证项目可正常构建（BUILD SUCCESSFUL）

---

## 五、空间释放统计

| 类别 | 释放空间 |
|------|----------|
| 调试/临时文件 | ~693 MB |
| 构建/运行产物 | ~1.3 GB |
| 未引用纹理 | 152 KB |
| **总计** | **约 2 GB** |

---

*本方案聚焦"删除无用代码/文件"，不涉及功能性代码重构。源代码经检查无死代码，整洁度良好，构建验证通过。*
